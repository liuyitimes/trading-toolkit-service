# 2026-07-01 Session Log

## 用户指令
"继续开发"（backtest回测包）

## 完成的工作

### 1. 修复多标的支持
- `engine.py` 之前只处理 `symbols[0]`，其余标的被忽略
- 改为按时间戳合并所有标的的bar，每个时间步处理所有标的的策略信号
- 使用 `itertools.groupby` 按时间戳分组

### 2. 添加T+1卖出限制
- `portfolio.py` 已有 `is_t1_available()` 但从未被engine调用
- 在engine的订单提交前加了T+1检查，当日买入的股票当日不可卖出

### 3. akshare改为lazy import
- `data.py` 顶部 `import akshare` 导致测试和mock场景直接报错
- 改为在 `load()` 和 `load_prev_close()` 方法内部import

### 4. 新增MockMarketData
- `backtest/mock_data.py` — 离线合成1分钟K线数据，支持指定seed复现
- 包含完整交易时段（集合竞价9:15-9:25、连续竞价、收盘竞价14:57-15:00）

### 5. 77个单元测试
- 新建 `tests/` 目录，覆盖全部8个模块
- `conftest.py` mock掉akshare依赖，测试无需联网
- 全部通过 `py -m pytest tests/ -v`

### 6. mock_demo示例
- `examples/mock_demo.py` — 基于MockData的策略示例

## 未做的事
- demo里单参与者回测无法产生成交（T+1正确阻止了同日买卖），这是预期行为，非bug
